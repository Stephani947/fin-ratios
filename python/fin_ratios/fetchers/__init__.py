"""
Optional data fetchers.

Each fetcher maps a data source's API into the standard fin_ratios types:
  IncomeStatement, BalanceSheet, CashFlowStatement, MarketData

Install optional deps:
  pip install fin-ratios[fetchers]   # httpx + yfinance
"""
